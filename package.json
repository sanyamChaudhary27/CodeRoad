{
  "name": "coderoad-root",
  "version": "1.0.0",
  "description": "Root of the CodeRoad project",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install",
    "start-frontend": "cd frontend && npm run dev",
    "start-backend": "cd backend && uvicorn app.app:app --reload --port 8000"
  }
}
