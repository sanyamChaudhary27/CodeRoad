HOW TO RUN CODE ROAD
====================

1. Start the backend (Terminal 1):

   cd d:\CodeRoad\backend
   .\venv\Scripts\activate

    python.exe -m uvicorn app.app:app --reload --host 0.0.0.0 --port 8000
   

2. Start the frontend (Terminal 2):

   cd d:\CodeRoad\frontend
   npm run dev

3. Open the app:

   Frontend:  http://localhost:5173
   API Docs:  http://localhost:8000/docs
   Health:    http://localhost:8000/health

Note: The SQLite database (backend/coderoad.db) persists between sessions,
so your accounts will still be there.

AFTER SWITCHING BRANCHES
========================
If you switch git branches and get database errors, run the migration:

   cd d:\CodeRoad\backend
   .\venv\Scripts\activate
   python _migrate_db.py

Then also install any new dependencies:

   pip install -r requirements.txt
